function [feature_Table, data_feature_Table] = extract_features(dataTable)
    % Esempio base: restituisce i dati originali senza modificarli.
    % Sostituisci questo codice con l'algoritmo di estrazione delle caratteristiche desiderato.
    feature_Table = dataTable;
    data_feature_Table = dataTable;
end
